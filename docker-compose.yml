# Single-shot service, triggered by an external scheduler via:
#   docker compose run --rm predict-co2
#
# Not meant to be started with "docker compose up -d" - there is no
# internal loop, and restart is intentionally disabled.

services:
  predict-co2:
    build: deployment/.
    environment:
      - HA_HOSTNAME=${HA_HOSTNAME}
      - HA_TOKEN=${HA_TOKEN}
      - HA_INPUT_ENTITY_ID=${HA_INPUT_ENTITY_ID}
      - HA_OUTPUT_ENTITY_ID=${HA_OUTPUT_ENTITY_ID}
      - HA_OUTPUT_PATH=/app/out/last_co2_values.csv
      - PREDICTION_OUTPUT_PATH=/app/out/predicted_co2.json
    volumes:
      - ./out:/app/out
    restart: "no"